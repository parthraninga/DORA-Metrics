PORT=3000

# Supabase (tokens table). Run in SQL Editor:
# create table tokens ( id uuid primary key default gen_random_uuid(), name text not null, token text not null, type text not null check (type in ('github','gitlab','bitbucket')), created_at timestamptz default now() );
SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Lambda URL for fetch data (optional; default in code)
# LAMBDA_FETCH_URL=https://your-lambda-url.lambda-url.region.on.aws/
